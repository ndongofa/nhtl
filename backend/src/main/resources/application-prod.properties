# =========================
# Server
# =========================
server.port=${PORT:8080}
server.servlet.context-path=/

# =========================
# Datasource (Supabase via Pooler)
# =========================

supabase.jwt.secret=${SUPABASE_JWT_SECRET}
supabase.service.role.key=${SUPABASE_SERVICE_ROLE_KEY}
supabase.anon-key=${SUPABASE_ANON_KEY}

spring.datasource.url=${SUPABASE_DB_URL}
spring.datasource.username=${SUPABASE_DB_USER}
spring.datasource.password=${SUPABASE_DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# D�sactivation ABSOLUE du cache de prepared statements
spring.datasource.hikari.pool-prepared-statements=false
spring.datasource.hikari.prepStmtCacheSize=0
spring.datasource.hikari.prepStmtCacheSqlLimit=0
spring.datasource.hikari.data-source-properties.cachePrepStmts=false
spring.datasource.hikari.data-source-properties.useServerPrepStmts=false

# Configuration du pool de connexions (Hikari)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# =========================
# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
# Le dialecte PostgreSQL est auto-d�tect�\u202F: commentaire laiss� pour clart�
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.h2.console.enabled=false

# =========================
# Logging
# =========================
logging.level.root=WARN
logging.level.com.nhtl=ERROR
logging.level.org.springframework=ERROR
logging.level.org.hibernate.SQL=ERROR
logging.level.org.springframework.core.env=WARN